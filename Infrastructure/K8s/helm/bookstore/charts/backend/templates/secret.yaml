apiVersion: v1
kind: Secret

metadata:
  name: {{ include "backend.fullname" . }}

type: Opaque

stringData:
  SPRING_DATASOURCE_USERNAME: {{ .Values.secret.dbUsername | quote }}
  SPRING_DATASOURCE_PASSWORD: {{ .Values.secret.dbPassword | quote }}
  JWT_SECRET: {{ .Values.secret.jwtSecret | quote }}