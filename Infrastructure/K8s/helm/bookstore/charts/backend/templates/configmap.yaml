apiVersion: v1
kind: ConfigMap

metadata:
  name: {{ include "backend.fullname" . }}

data:
  SPRING_PROFILES_ACTIVE: {{ .Values.config.springProfile | quote }}

  SPRING_DATASOURCE_URL: "jdbc:postgresql://{{ .Release.Name }}-postgresql:5432/{{ .Values.config.database.name }}"